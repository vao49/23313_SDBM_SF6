{% extends 'base.html.twig' %}

{% block title %}
	{% trans %}Edit Article{% endtrans %}
{% endblock %}

{% block body %}
	<h1>{% trans %}Edit Article{% endtrans %}</h1>

	{{ include('article/_form.html.twig', {'button_label': 'Update'}) }}

	<a href="{{ path('app_article_index') }}">
		<button class="btn btn-warning  {% trans %}icon back{% endtrans %}">
			{{ button_label|default('back to list')|trans }}
		</button>
	</a>

	{# {{ include('article/_delete_form.html.twig') }} #}
{% endblock %}
