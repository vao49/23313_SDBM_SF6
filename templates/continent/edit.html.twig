{% extends 'base.html.twig' %}

{% block title %}
	{% trans %}Edit Continent{% endtrans %}
{% endblock %}

{% block body %}
	<h1>{% trans %}Edit Continent{% endtrans %}</h1>

	{{ include('continent/_form.html.twig', {'button_label': 'Update'|trans}) }}

	<a
		href="{{ path('app_continent_index') }}">
		{# {% trans %}back to list{% endtrans %} #}
		<button class="btn btn-warning  {% trans %}icon back{% endtrans %}">
			{{ button_label|default('back to list')|trans }}
		</button>
	</a>

	{# {{ include('continent/_delete_form.html.twig') }} #}
{% endblock %}
